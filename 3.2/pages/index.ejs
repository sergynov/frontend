<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
  <title>Node simple server</title>
</head>
<body>
  <div class="container">
    <h1><%=title%></h1>

    <form action="/" method="post" class="mb-2">
      <div class="form-group mb-2" >
        <input type="text" class="form-control" name="title" required>
      </div>
      
      <button class="btn btn-primary" type="submit">Create</button>
    </form>

    <% if(notes.length) { %>
    <div class="card">
      <ul class="list-group">
        <% for(let i=0; i < notes.length; i++) { %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <span class="note-title"><%= notes[i].title %></span>
          
          <div class="btn-box">
            <button class="btn-edit" data-type="edit" data-id="<%= notes[i].id %>" data-title="<%= notes[i].title %>" >Edit</button>
            <button class="btn-delete" data-type="remove" data-id="<%= notes[i].id %>" >X</button>
          </div>
        </li>
        
        <% } %>
      </ul>
    </div>
    <% } else { %>
      <p>No notes!</p>
      <% } %>
  </div>


  <script src="/app.js"></script>
</body>
</html>